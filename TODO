* Add a command line parameter to GnomeMeeting to pass the IP address to contact.
* Fix VideoGrabber
* Rework the ILS window
* Fix the is_cam (see if the video device list is empty or not), and if it is, just disable the appropriate frames.
* Add gconf support
* check that it doesn't register if options are changed and the check box not checked