See our wiki: http://wiki.ekiga.org/index.php/ToDo
1) Display "calling XYZ" when placing a call
2) Rework incoming call notifications to use the new call window
3) Rework presence icons : BUSY / DND
4) Fix Call window placement on preview: DONE
5) Rework in call information display in the call window
  (most notably, show codecs used)
6) Close call window in preview mode when pressing X close button
7) Problems with zoom in in resolutions other than QCIF
8) After several calls, the call window shows only the video,
  without any other gtk component
9) In call window, show tooltip only on some components, not on
  the whole window
